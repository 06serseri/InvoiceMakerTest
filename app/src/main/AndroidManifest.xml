<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.s3cilabs.invoicemakertest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InvoiceMakerTest">
        <activity android:name=".Company.CompanyActivity"
            android:parentActivityName=".MainActivity"
            android:label="Company Information"></activity>
        <activity
            android:name=".Invoice.EditInvoiceActivity"
            android:label="Update Invoice"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Invoice.AddInvoiceActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>